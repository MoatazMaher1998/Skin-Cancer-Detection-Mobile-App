import 'package:flutter/material.dart';
const KTextFormField = InputDecoration(
  border: OutlineInputBorder(),
  labelText: 'Update the label Text with CopyWith',
);
const colorizeColors = [
  Colors.purple,
  Colors.blue,
  Colors.yellow,
  Colors.red,
];
const KTextStyleCancer = TextStyle(
fontFamily: 'Zen Dots',
fontSize: 20,
fontWeight: FontWeight.bold,
color: Colors.teal);

